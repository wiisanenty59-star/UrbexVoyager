kind = "web"
previewPath = "/"
title = "Voyager"
version = "1.0.0"
id = "artifacts/voyager"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19286

[services.development]
run = "pnpm --filter @workspace/voyager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/voyager", "run", "build" ]
publicDir = "artifacts/voyager/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19286"
BASE_PATH = "/"
